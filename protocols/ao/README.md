# AO 协议

本目录用于放置 AO 协议代码。

AO 协议是系统权威状态来源，负责身份、小说、标签、评分、论坛、任务、治理、代币、声誉、处罚和数据提供商规则。

协议代码应拆分为一组协作进程，避免形成单个巨型进程。评论、评分、标签、投票和论坛等高频操作应由独立业务进程处理，DID Registry 只处理低频身份操作。

第一阶段 `DIDRegistry`、`DAO`、`Token`、`Task`、`Reputation` 的消息规格和权限边界见 [第一阶段最小可运行闭环设计](../../apps/web/docs/第一阶段最小可运行闭环设计.md)。
