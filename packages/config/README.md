# 共享配置包

本目录用于放置跨应用复用的配置定义。

共享配置可以包含网络环境、网关列表、协议进程标识、构建约定和测试环境默认值等内容。

配置包只保存可复用配置，不应放置应用私有逻辑或运行时密钥。

第一阶段测试代币、DAO 每日增发规则、ARNS 测试入口、挑战期、裁决者白名单、网关索引间隔和测试数据保留策略见 [第一阶段最小可运行闭环设计](../../apps/web/docs/第一阶段最小可运行闭环设计.md)。
